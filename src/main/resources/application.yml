server:
  port: 8080
spring:
  datasource:
    driver-class-name: dm.jdbc.driver.DmDriver
    url: jdbc:dm://127.0.0.1:5236?schema=SYSDBA&useUnicode=true&characterEncoding=GBK
    username: SYSDBA
    password: Root123456
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false
    db-config:
      id-type: AUTO

# SpringDoc OpenAPI (Swagger 3) 配置
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  packages-to-scan: com.project.phm.controller
  default-flat-param-object: true

# 支持系统 API 路径配置（base URL 存于 DB external_platform 表）
support-system:
  paths:
    getDzgxxx: /api/v1/config/dzgxxx
